import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.msa.capacourse',
  appName: 'capacitorCourse',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
